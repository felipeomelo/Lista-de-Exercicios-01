let distancia = 5000;
let tempo = 2200;

let velocidade = distancia / tempo;
let velocidadeMedia = velocidade * (3.6);

console.log(`A sua Velocidade Média na corrida de hoje foi de: ${velocidadeMedia.toFixed(2)} Km/h`);

// variável.toFixed(2)